<!doctype html>
<html lang="pt-br">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Matrícula #<%= matricula.id %> • Academia Infinity</title>
  <link rel="stylesheet" href="/painel/css/listamatriculas.css">
</head>
<body>
  <main class="page">
    <section class="card">
      <h1 class="title">Matrícula #<%= matricula.id %></h1>
      <ul class="matricula-list">
        <li><strong>Nome completo:</strong> <span><%= matricula.nome_completo %></span></li>
        <li><strong>Endereço:</strong> <span><%= matricula.endereco %></span></li>
        <li><strong>CEP:</strong> <span><%= matricula.cep %></span></li>
        <li><strong>CPF:</strong> <span><%= matricula.cpf %></span></li>
        <li><strong>WhatsApp:</strong> <span><%= matricula.whatsapp %></span></li>
        <li><strong>Data de nascimento:</strong> <span><%= matricula.data_nascimento %></span></li>
        <li><strong>Plano:</strong> <span><%= matricula.plano %></span></li>
        <li><strong>Objetivo:</strong> <span><%= matricula.objetivo || "—" %></span></li>
        <li><strong>Origem:</strong> <span><%= matricula.origem || "—" %></span></li>
        <li><strong>Data de envio:</strong> <span><%= matricula.submitted_at ? matricula.submitted_at.toLocaleString("pt-BR") : "—" %></span></li>
      </ul>
    </section>
  </main>
  <script src="/painel/js/listamatriculasScripts.js"></script>
</body>
</html>
