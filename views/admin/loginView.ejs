<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Login | Sistema Infinity</title>
  <link rel="stylesheet" href="/css/admin/adminGlobalStyles.css" />
  <style>
    body { display: flex; align-items: center; justify-content: center; min-height: 100vh; background: var(--bg,#0f1115);}
    .login-card {
      background: var(--surface,#171a21);
      border-radius: 18px;
      box-shadow: 0 8px 32px rgba(0,0,0,.25);
      padding: 2.5rem 2rem 2rem 2rem;
      max-width: 340px;
      width: 100%;
      color: var(--text,#e8eaed);
      display: flex;
      flex-direction: column;
      gap: 1.2rem;
    }
    .login-card h1 {
      margin: 0 0 0.5rem 0;
      font-size: 1.6rem;
      font-weight: 800;
      text-align: center;
      letter-spacing: .5px;
    }
    .login-card form { display: flex; flex-direction: column; gap: 1rem; }
    .login-card label { font-size: 1rem; font-weight: 600; }
    .login-card input[type="email"],
    .login-card input[type="password"] {
      width: 100%;
      padding: 10px 12px;
      border-radius: 10px;
      border: 1px solid var(--border,#262a33);
      background: #12161f;
      color: var(--text,#e8eaed);
      font: inherit;
      margin-bottom: 0.5rem;
    }
    .login-card input[type="email"]:focus,
    .login-card input[type="password"]:focus {
      outline: none;
      border-color: var(--primary,#CF1742);
      box-shadow: 0 0 0 2px color-mix(in oklab, var(--primary) 22%, transparent);
    }
    .login-card button[type="submit"] {
      background: var(--primary,#CF1742);
      color: #fff;
      border: none;
      border-radius: 10px;
      padding: .7rem 0;
      font-size: 1.08rem;
      font-weight: 700;
      cursor: pointer;
      margin-top: 0.5rem;
      transition: filter .18s;
    }
    .login-card button[type="submit"]:hover { filter: brightness(1.08);}
    .login-card .error {
      color: #e11d48;
      background: #2a0a16;
      border-radius: 8px;
      padding: 8px 12px;
      font-size: 1rem;
      text-align: center;
      margin-bottom: 0.5rem;
    }
    .login-card .logo {
      display: flex;
      justify-content: center;
      margin-bottom: 1.2rem;
    }
    .login-card .logo img {
      max-width: 120px;
      height: auto;
      display: block;
    }
  </style>
</head>
<body>
  <div class="login-card">
    <div class="logo">
      <img src="/media/LOGO-INF.png" alt="Logo Infinity" />
    </div>
    <h1>Login</h1>
    <% if (typeof error !== 'undefined' && error) { %>
      <div class="error"><%= error %></div>
    <% } %>
    <form method="POST" action="/login">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>">
      <input type="hidden" name="to" value="<%= to || '/admin' %>">
      <label for="email">E-mail</label>
      <input id="email" name="email" type="email" required autocomplete="username" autofocus placeholder="Seu e-mail" />
      <label for="senha">Senha</label>
      <input id="senha" name="senha" type="password" required autocomplete="current-password" placeholder="Sua senha" />
      <button type="submit">Entrar</button>
    </form>
  </div>
</body>
</html>
